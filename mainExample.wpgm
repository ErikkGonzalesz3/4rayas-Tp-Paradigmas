import wollok.game.*
import tablero.* 
import juego.*

program cuatroEnRaya {
	game.title("4 en Raya")
	game.width(25)
	game.height(15)

	juego.iniciarJuego()
	(1..7).forEach {numero => keyboard.num(numero).onPressDo{juego.quiereJugar(numero) }}

	game.start()
}
